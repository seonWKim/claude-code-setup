# <%= answers.projectName %> - Claude Code Configuration

This project is configured with Claude Code for AI-assisted development.

## Project Overview

- **Type**: <%= answers.projectType %>
- **Goal**: <%= answers.projectGoal %>
- **Security Level**: <%= answers.securityLevel %>

## Tech Stack

<% const stack = getTechStackSummary(answers); %>
<% stack.forEach(function(item) { %>
- <%= item %>
<% }); %>

## Available Commands

<% const commands = getAvailableCommands(components); %>
<% if (commands.length > 0) { %>
<% commands.forEach(function(cmd) { %>
- `<%= cmd %>`
<% }); %>
<% } else { %>
No custom commands configured.
<% } %>

## Critical Rules

<% const rules = getCriticalRules(components); %>
<% if (rules.length > 0) { %>
The following rules are **required** for this project:

<% rules.forEach(function(rule) { %>
- **<%= rule.name %>**<% if (rule.required) { %> (enforced)<% } %>
<% }); %>
<% } else { %>
No critical rules configured.
<% } %>

## Code Style

<% if (answers.backendLanguage === 'typescript' || answers.hasFrontend) { %>
- Use TypeScript for type safety
- Follow ESLint/Prettier configurations
<% } %>
<% if (answers.stylingApproach === 'tailwind') { %>
- Use Tailwind CSS utility classes
<% } %>
- Write self-documenting code with clear naming
- Keep functions small and focused

## Testing Requirements

<% if (answers.testingApproach === 'tdd') { %>
- **TDD Required**: Write tests before implementation
- Run tests with each change
<% } else if (answers.testingApproach === 'standard') { %>
- Write tests alongside implementation
- Maintain reasonable coverage
<% } else if (answers.testingApproach === 'minimal') { %>
- Test critical paths only
<% } %>
<% if (answers.coverageTarget !== 'none') { %>
- Target coverage: <%= answers.coverageTarget %>%
<% } %>
<% if (answers.e2eFramework !== 'none') { %>
- E2E testing with <%= answers.e2eFramework %>
<% } %>

## Security Guidelines

<% if (answers.securityLevel === 'high') { %>
**HIGH SECURITY PROJECT**

- All code changes require security review
- Never commit secrets or credentials
- Validate all inputs
- Use parameterized queries
- Follow OWASP guidelines
<% if (answers.hasPayments) { %>
- Payment handling requires extra scrutiny
<% } %>
<% if (answers.hasBlockchain) { %>
- Smart contract interactions need security review
<% } %>
<% } else if (answers.securityLevel === 'medium') { %>
- Follow security best practices
- Never commit secrets
- Validate user inputs
<% } %>

## Git Workflow

<% if (answers.teamSize !== 'solo') { %>
- Branch strategy: <%= answers.gitWorkflow %>
<% if (answers.requireCodeReview) { %>
- Code review required before merge
<% } %>
<% } else { %>
- Solo development workflow
<% } %>

## MCP Integrations

<% if (components.mcpServers.length > 0) { %>
The following MCP servers are configured:

<% components.mcpServers.forEach(function(server) { %>
- **<%= server.name %>**: <%= server.config.description %>
<% }); %>

Configure API keys in `.claude.json` before using.
<% } else { %>
No MCP integrations configured.
<% } %>

---

*Generated by claude-code-setup*
